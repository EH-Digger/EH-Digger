Function +	Removed Check Condition 
add_opal_mem_range	if (!dn)
twr_p1025_setup_arch	if (np)
mvme5100_add_bridge	if (hose == NULL)
storcenter_add_bridge	if (hose == NULL)
gef_pic_init	if (gef_pic_irq_host == NULL)
get_immrbase	if (soc)
add_one_sriov_vf_pdn	if (!pdn)
pseries_pci_sriov_enable	if (!pdn->pe_num_map)
kasan_init_shadow_8M	if (!new)
opal_prd_probe	if (rc)
pika_setup_critical_temp	if (!irq)
opal_async_wait_response_interruptible	if (!msg)
flipper_pic_init	if (!pi)
mpc83xx_km_setup_arch	if (np != NULL)
mpc85xx_mds_qe_init	if (!guts)
imc_pmu_create	if (ret)
ep88xc_setup_arch	if (!np)
mpc5200_setup_xlb_arbiter	if (!xlb)
pcibios_map_io_space	if (bus->self)
macio_do_gpio_irq_disable	if (!irq)
add_var	if (!kobj)
pmc_probe	if (ret)
hlwd_pic_init	if (!irq_domain)
mpc8xx_pic_init	if (ret)
ppc4xx_l2c_probe	if (!np)
maple_find_nvram_base	if (rtcs)
icp_hv_get_xirr	if (rc == H_SUCCESS)
pseries_suspend_begin	if (rc)
kasan_init_shadow_page_tables	if (!new)
tsi108_eth_of_init	if (ret)
